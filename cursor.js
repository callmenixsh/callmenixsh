// // Select the glare element
// const glare = document.querySelector('.cursor-glare');

// // Update the position on mouse move
// document.addEventListener('mousemove', (e) => {
//   glare.style.transform = `translate(${e.clientX - glare.offsetWidth / 2}px, ${e.clientY - glare.offsetHeight / 2}px)`;
// });

// // Maintain glare visibility during scroll
// document.addEventListener('scroll', () => {
//   glare.style.opacity = '0.9';
// });
